plugins {
    id 'cpp-application'
    id 'cpp-unit-test'
}

application {
    targetMachines = [
        machines.windows.x86_64, machines.windows.x86, 
        machines.linux.x86_64,
        machines.macOS.x86_64
     ]
}
