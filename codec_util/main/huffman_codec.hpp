#ifndef HUFFMAN_CODEC_
#define HUFFMAN_CODEC_

#include "codec_util.hpp"

const codec_data huffman_endcode (const codec_data &);

const codec_data huffman_decode (const codec_data &);

#endif // HUFFMAN_CODEC_